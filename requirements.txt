# === PyTorch (CUDA 빌드는 머신에 맞게 별도 설치 권장) ===
#   로컬 RTX4060(Ada) = cu121/cu124, 서버 RTX5090(Blackwell) = cu128.
#   예) pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch>=2.4.0
torchvision>=0.19.0

# === VLM (현재 상황 분석: 기본 Qwen3-VL-2B-Instruct, env 로 Qwen2.5-VL-3B 교체 가능) ===
transformers>=4.49.0
qwen-vl-utils>=0.0.8
accelerate>=1.0.0
sentencepiece
einops
lm-format-enforcer>=0.10.0   # JSON 스키마 강제 디코딩(verify 출력)

# === 영상/이미지 처리 ===
opencv-python-headless
pillow
numpy

# === RAG (전조 검색: ChromaDB + BAAI/bge-m3) ===
chromadb>=0.5.0
sentence-transformers>=3.0.0

# === API 서버 ===
fastapi
uvicorn[standard]
python-multipart

# === 선택: 8GB 미만 GPU 4bit 폴백 ===
# bitsandbytes>=0.43.0
